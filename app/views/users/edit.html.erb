<%= form_for @user, html: { multipart: true } do |t| %>
  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :email %>
  <%= t.text_field :email %>

  <%= t.label :image%>
  <%= t.file_field :image %>

  <%= t.submit class: "button" %>
<% end %>
